[colors]
background = #101510
background-alt = #2f343f
foreground = #f3f4f5
foreground-alt = #676E7D
primary = #ffb52a
secondary = #e60053
alert = #E53935

[progress]
indicator = │
fill = ─

[ramp]
0 = ▁
0-foreground = #aaff77
1 = ▂
1-foreground = #aaff77
2 = ▃
2-foreground = #aaff77
3 = ▄
3-foreground = #aaff77
4 = ▅
4-foreground = #fba922
5 = ▆
5-foreground = #fba922
6 = ▇
6-foreground = #ff5555
7 = █
7-foreground = #ff5555

[bar/default]
width = 100%
fixed-center = false

background = ${colors.background}
foreground = ${colors.foreground}

font-0 = sans:pixelsize=12:1
font-1 = monospace:pixelsize=12:1
font-2 = FontAwesome5Brands:style=solid:pixelsize=12:1
font-3 = FontAwesome5Free:style=solid:pixelsize=12:1

padding-left = 0
padding-right = 2

module-margin-left = 1
module-margin-right = 2

[bar/bar1]
inherit = bar/default
height = 27

line-size = 3

modules-left = i3
modules-right = cpu memory filesystem pulseaudio wlan backlight-acpi battery temperature date

tray-position = right
tray-padding = 2

[module/filesystem]
type = internal/fs
interval = 25
mount-0 = /
mount-1 = /home

label-mounted =  %{F#0a81f5}%mountpoint%%{F-}: %free%
label-unmounted =

[module/i3]
type = internal/i3

; DRY
workspace-padding = 2

label-mode-padding = 2
label-mode-foreground = ${colors.foreground}
label-mode-background = ${colors.primary}

; focused = Active workspace on focused monitor
label-focused-background = ${colors.background-alt}
label-focused-underline= ${colors.primary}
label-focused-padding = ${self.workspace-padding}

; unfocused = Inactive workspace on any monitor
label-unfocused-padding = ${self.workspace-padding}

; visible = Active workspace on unfocused monitor
label-visible-background = ${self.label-focused-background}
label-visible-underline = ${self.label-focused-underline}
label-visible-padding = ${self.workspace-padding}

; urgent = Workspace with urgency hint set
label-urgent-underline = ${colors.alert}
label-urgent-padding = ${self.workspace-padding}

; label-separator = |

[module/xbacklight]
type = internal/xbacklight

format = <label>
label =  %percentage%%
label-underline = #234567

[module/backlight-acpi]
inherit = module/xbacklight
type = internal/backlight
card = intel_backlight

[module/cpu]
type = internal/cpu
format = <label> <ramp-load>
interval = 2
label =  %percentage:2%%

ramp-load-0 = ${ramp.0}
ramp-load-0-foreground = ${ramp.0-foreground}
ramp-load-1 = ${ramp.1}
ramp-load-1-foreground = ${ramp.1-foreground}
ramp-load-2 = ${ramp.2}
ramp-load-2-foreground = ${ramp.2-foreground}
ramp-load-3 = ${ramp.3}
ramp-load-3-foreground = ${ramp.3-foreground}
ramp-load-4 = ${ramp.4}
ramp-load-4-foreground = ${ramp.4-foreground}
ramp-load-5 = ${ramp.5}
ramp-load-5-foreground = ${ramp.5-foreground}
ramp-load-6 = ${ramp.6}
ramp-load-6-foreground = ${ramp.6-foreground}
ramp-load-7 = ${ramp.7}
ramp-load-7-foreground = ${ramp.7-foreground}

[module/memory]
type = internal/memory
interval = 2
format = <label> <ramp-used>
label =  %gb_free%

ramp-used-0 = ${ramp.0}
ramp-used-0-foreground = ${ramp.0-foreground}
ramp-used-1 = ${ramp.1}
ramp-used-1-foreground = ${ramp.1-foreground}
ramp-used-2 = ${ramp.2}
ramp-used-2-foreground = ${ramp.2-foreground}
ramp-used-3 = ${ramp.3}
ramp-used-3-foreground = ${ramp.3-foreground}
ramp-used-4 = ${ramp.4}
ramp-used-4-foreground = ${ramp.4-foreground}
ramp-used-5 = ${ramp.5}
ramp-used-5-foreground = ${ramp.5-foreground}
ramp-used-6 = ${ramp.6}
ramp-used-6-foreground = ${ramp.6-foreground}
ramp-used-7 = ${ramp.7}
ramp-used-7-foreground = ${ramp.7-foreground}

[module/wlan]
type = internal/network
interface = wlp60s0
interval = 3.0

format-connected = <label-connected>
label-connected =  %upspeed%  %downspeed%

format-disconnected = <label-disconnected>
label-disconnected = disconnected
label-disconnected-foreground = ${colors.foreground-alt}

[module/date]
type = internal/date
interval = 1

date = %a %b %d
time = %H:%M:%S

label =  %date%   %time%

[module/pulseaudio]
type = internal/pulseaudio

format-volume = <ramp-volume> <label-volume>
label-volume = %percentage%%

label-muted = 
label-muted-foreground = ${colors.foreground-alt}

ramp-volume-0 = 
ramp-volume-1 = 
ramp-volume-2 = 

[module/battery]
type = internal/battery
battery = BAT0
adapter = AC
full-at = 99
time-format = %H:%M

format-charging = <animation-charging> <label-charging>
label-charging = %percentage%% (%time%)

format-discharging = <ramp-capacity> <label-discharging>
label-discharging = %percentage%% (%time%)

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 

animation-charging-0 = 
animation-charging-1 = 
animation-charging-2 = 
animation-charging-3 = 
animation-charging-4 = 
animation-charging-foreground = #117744
animation-charging-framerate = 750

[module/temperature]
type = internal/temperature
warn-temperature = 60

format = <ramp> <label>
format-warn = <ramp> <label-warn>

label = %temperature-c%
label-warn = %temperature-c%
label-warn-foreground = ${colors.alert}

ramp-0 = 
ramp-1 = 

[settings]
screenchange-reload = true

[global/wm]
margin-top = 0
margin-bottom = 0

; vim:ft=dosini
